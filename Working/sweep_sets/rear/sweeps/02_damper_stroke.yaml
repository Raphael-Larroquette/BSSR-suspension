# 02 - REAR DRIVEN BY DAMPER LENGTH. The inverse of 01: find the usable wheel travel
# for the damper you actually have, which sets the ranges in 01.
# Ranges and step counts are overridden by run.yaml. See ../../SWEEPS.md.
version: 1
steps: 41
targets:
  - type: element_length
    element: damper
    side: center
    mode: relative
    start: -30
    stop: 30
